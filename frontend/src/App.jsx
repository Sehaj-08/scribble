// Defines the root React component that currently serves as the application's
// temporary shell before the real pages and routing are added.
function App() {
  return (
    <div className="app">
      <h1>Scribble</h1>
    </div>
  )
}

export default App
